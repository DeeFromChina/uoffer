<sqls>
	<!-- 用户历史模板 -->
	<sql id="queryUserExperienceName">
		SELECT
			detail.id,
			(
				CASE 
					WHEN detail.ename = detail.sname 
					THEN 
						IF(
							@named = (select @named := detail.ename),
							CONCAT(detail.sname,(@i := @i + 1)),
							CONCAT(detail.sname,(@i := 1))
						) 
					ELSE "" END 
			) AS dname 
			FROM
				(
					SELECT
						experience.id,
						experience.name AS ename,
						sube.name AS sname
					FROM
						user_experience experience
					LEFT JOIN (
							SELECT
								experience.name
							FROM
								user_experience experience
							GROUP BY
								experience.name
							HAVING
								count(experience.name) > 1
						) sube ON experience.name = sube.name {#param}
						ORDER BY ename DESC
				) detail,
				(SELECT @i := 0) AS it 
	</sql>
	<sql id="queryUserExperience">
		SELECT
			e.id AS id,
			e.name AS name,
			e.type AS type,
			e.start_time AS start_time,
			e.end_time AS endTime,
			e.job AS job,
			j.name AS jobName,
			e.job_description AS jobDescription,
			e.job_belong AS jobBelong,
			e.level AS level,
			e.descript1 AS descript1,
			e.descript2 AS descript2 
		FROM
			user_experience e
		LEFT JOIN job j ON j.id = e.job {#param}
	</sql>
</sqls>